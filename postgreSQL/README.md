Para executar use:

`docker-compose up -d`

Para acessa o banco de dados criado:

acesse com o DBiver e crie uma nova conecao, se o docker estiver na maquina local os dados seram esses:

URL: `jdbc:postgresql://localhost:5432/mydb`

Usuario: user

Senha: password
